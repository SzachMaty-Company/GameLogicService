external:
  service:
    user-service:
      url: ${USER_SERVICE_URL}
    ai-service:
      url: ai-service

  data:
    redis:
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
      host: ${REDIS_HOST_NAME}

jwt:
  key: ${JWT_KEY}